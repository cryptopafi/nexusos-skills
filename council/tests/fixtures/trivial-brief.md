# Rename variable

Should I rename the local variable `foo` to `bar` in the `process_data()` helper
function inside `utils.py`? The variable is only used in two lines and the change
would not affect any external interface or public API.
