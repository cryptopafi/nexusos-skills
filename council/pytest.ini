[pytest]
testpaths = tests
markers =
    live: marks tests requiring real API keys (deselected by default)
    preflight_unit: marks tests that directly exercise _preflight_provider_check (bypass autouse patch)
